name: builder-space-eks
description: EKS Development Environment - Modular Python deployment with cost optimization
runtime:
  name: python
  options:
    toolchain: pip
    virtualenv: venv
config:
  aws:region:
    description: AWS region for resources
    default: af-south-1
