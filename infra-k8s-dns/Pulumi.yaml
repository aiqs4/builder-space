name: eks-dns-zone
description: EKS DNS subdomain zone
runtime:
  name: python
  options:
    toolchain: pip
    # virtualenv: venv
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
